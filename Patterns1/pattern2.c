#include <stdio.h>
#include "pattern2.h"

#if 0
uint16_t drawLeftPyramide(uint16_t n)
{
    for(uint16_t i_line = 1; i_line <= n; i_line++)
    {
        for (uint8_t i_col = 1; i_col <= i_line; i_col++)
        {
            printf("* ");
        }
        printf("\n");

    }
    return 0;
}
#endif // 0

